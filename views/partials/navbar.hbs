<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="/"><img src="/logo.png" alt="Calendar"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav">
                {{#if isAuth}}
                <li class="nav-item">
                    <a class="nav-link {{#if isMain }}active{{/if}}" aria-current="page" href="/">Main</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if isProfile }}active{{/if}}" href="/profile">Profile</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/auth/logout">Logout</a>
                </li>
                {{else}}
                <li class="nav-item">
                    <a class="nav-link {{#if isLogin }}active{{/if}}" href="/auth/login">Sign In</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if isRegister }}active{{/if}}" href="/auth/register">Sign Up</a>
                </li>
                {{/if}}
            </ul>
        </div>
    </div>
</nav>